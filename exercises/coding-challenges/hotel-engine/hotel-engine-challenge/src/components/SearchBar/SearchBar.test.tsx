import SearchBar from './SearchBar';

import { render, screen } from '@testing-library/react';

describe('SearchBar Component', () => {
  /*
  it('should render input with "" as value', () => {
    render(<SearchBar initialInputValue={''} />);
    const input = screen.getByLabelText(/search posts/i);
    expect(input).toHaveValue('');
  });
  it('should render input with value "Hello" when passed as prop', () => {
    render(<SearchBar initialInputValue={'Hello'} />);
    const input = screen.getByLabelText(/search posts/i);
    expect(input).toHaveValue('Hello');
  });*/
});
